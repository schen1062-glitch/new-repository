导入后请执行：

1. Publish（路径冲突停用旧工作流）
2. 用下面 Console 测试（替换地址）：

fetch("https://nonparentally-unfagged-huong.ngrok-free.dev/webhook/compliance-audit", {
  method: "POST",
  headers: { "Content-Type": "application/json" },
  body: JSON.stringify({
    operation: "delete",
    user: "admin",
    entity: "客户记录"
  })
}).then(r => r.text()).then(console.log).catch(console.error);